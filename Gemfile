source 'https://rubygems.org'
gem 'github-pages', group: :jekyll_plugins
gem 'tzinfo-data'
gem "webrick"
gem 'wdm', '>= 0.1.0' if Gem.win_platform?
gem 'fiddle' # In windows, you might need to install `pacman -S mingw-w64-ucrt-x86_64-libffi`. You will need to do that in the MSYS console (possibly as administrator) (in <ruby_path>\msys64\msys2.exe)
